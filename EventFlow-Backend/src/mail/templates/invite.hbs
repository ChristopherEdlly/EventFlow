<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Convite para Evento</title>
    <style>
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
            line-height: 1.6;
            color: #333333;
            background-color: #f4f4f7;
            margin: 0;
            padding: 0;
        }
        .container {
            max-width: 600px;
            margin: 40px auto;
            background-color: #ffffff;
            border-radius: 12px;
            overflow: hidden;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }
        .header {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            padding: 40px 30px;
            text-align: center;
            color: #ffffff;
        }
        .header h1 {
            margin: 0;
            font-size: 28px;
            font-weight: 700;
        }
        .content {
            padding: 40px 30px;
        }
        .greeting {
            font-size: 18px;
            margin-bottom: 20px;
            color: #1a202c;
        }
        .event-details {
            background-color: #f7fafc;
            border-left: 4px solid #667eea;
            padding: 20px;
            margin: 25px 0;
            border-radius: 8px;
        }
        .event-title {
            font-size: 24px;
            font-weight: 700;
            color: #2d3748;
            margin: 0 0 15px 0;
        }
        .detail-row {
            display: flex;
            margin-bottom: 12px;
            align-items: flex-start;
        }
        .detail-icon {
            margin-right: 12px;
            color: #667eea;
            font-size: 18px;
        }
        .detail-label {
            font-weight: 600;
            color: #4a5568;
            min-width: 80px;
        }
        .detail-value {
            color: #2d3748;
        }
        .description {
            margin: 20px 0;
            padding: 15px;
            background-color: #ffffff;
            border-radius: 6px;
            color: #4a5568;
            line-height: 1.8;
        }
        .cta-button {
            display: inline-block;
            padding: 14px 32px;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: #ffffff !important;
            text-decoration: none;
            border-radius: 8px;
            font-weight: 600;
            font-size: 16px;
            text-align: center;
            margin: 25px 0;
            box-shadow: 0 4px 6px rgba(102, 126, 234, 0.3);
            transition: transform 0.2s;
        }
        .cta-button:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 12px rgba(102, 126, 234, 0.4);
        }
        .footer {
            background-color: #f7fafc;
            padding: 30px;
            text-align: center;
            font-size: 14px;
            color: #718096;
            border-top: 1px solid #e2e8f0;
        }
        .footer p {
            margin: 5px 0;
        }
        .divider {
            height: 1px;
            background-color: #e2e8f0;
            margin: 25px 0;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>🎉 Você foi convidado!</h1>
        </div>

        <div class="content">
            <p class="greeting">Olá {{guestName}},</p>

            <p>{{organizerName}} gostaria de convidar você para participar do seguinte evento:</p>

            <div class="event-details">
                <h2 class="event-title">{{eventTitle}}</h2>

                <div class="detail-row">
                    <span class="detail-icon">📅</span>
                    <div>
                        <span class="detail-label">Data:</span>
                        <span class="detail-value">{{eventDate}}</span>
                    </div>
                </div>

                <div class="detail-row">
                    <span class="detail-icon">🕐</span>
                    <div>
                        <span class="detail-label">Horário:</span>
                        <span class="detail-value">{{eventTime}}</span>
                    </div>
                </div>

                <div class="detail-row">
                    <span class="detail-icon">📍</span>
                    <div>
                        <span class="detail-label">Local:</span>
                        <span class="detail-value">{{eventLocation}}</span>
                    </div>
                </div>

                <div class="detail-row">
                    <span class="detail-icon">👤</span>
                    <div>
                        <span class="detail-label">Organizador:</span>
                        <span class="detail-value">{{organizerName}}</span>
                    </div>
                </div>
            </div>

            {{#if eventDescription}}
            <div class="description">
                <strong>Sobre o evento:</strong><br>
                {{eventDescription}}
            </div>
            {{/if}}

            <div class="divider"></div>

            <p style="text-align: center; margin: 30px 0 20px 0;">
                <strong>Por favor, confirme sua presença:</strong>
            </p>

            <div style="text-align: center;">
                <a href="{{rsvpUrl}}" class="cta-button">
                    Confirmar Presença
                </a>
            </div>

            <p style="font-size: 14px; color: #718096; margin-top: 30px;">
                Clique no botão acima para visualizar os detalhes completos do evento e confirmar sua presença.
            </p>
        </div>

        <div class="footer">
            <p><strong>EventFlow</strong> - Simplifique a gestão dos seus eventos</p>
            <p>© {{year}} EventFlow. Todos os direitos reservados.</p>
            <p style="margin-top: 15px; font-size: 12px;">
                Este é um email automático, por favor não responda diretamente.
            </p>
        </div>
    </div>
</body>
</html>
